{
  "name": "monolithflow",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "bun --filter back-end run dev & bun --filter client run dev",
    "dev:backend": "bun --filter back-end run dev",
    "dev:frontend": "bun --filter client run dev"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "drizzle-orm": "^1.0.0-rc.4",
    "zod": "^4.4.3"
  }
}
