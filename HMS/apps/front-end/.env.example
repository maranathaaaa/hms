# Absolute origin of the backend API. Better Auth appends `/api/auth` to it.
# Leave empty for local web dev to use the Vite proxy (`/api` -> localhost:3001).
# Must be set in the Vercel project's Environment Variables — Vercel does not
# read this file at build time.
VITE_API_URL=https://nexacare-xppe.onrender.com
